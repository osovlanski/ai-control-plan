# context-continuation fixture

A deliberately plain repository for the K11 `context-pressure` scenario: a git
repo with no package manifest, so no project verification is discovered. The
scenario is about the continuation control path — critical pressure, checkpoint,
`YIELDED(context)`, a clean successor from that exact checkpoint — not about
verification, which has its own scenarios.
